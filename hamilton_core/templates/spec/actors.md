# Actors

Actors are external entities that interact with the system; they define its
boundary. Write each as a `## A-nnnn` block. The worked
example below sits in a fenced code block; write your own below it.

```markdown
## A-0001
Name: End User
Description: Uses the application through the web interface.
```
